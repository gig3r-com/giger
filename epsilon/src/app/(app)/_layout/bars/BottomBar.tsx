import React from 'react';
import { Box } from '@mui/material';

export function BottomBar() {
    return (
        <Box sx={ { flex: 0, py: 1, pb: 1 } }>
            { `Bottom > Bar > and > path` }
        </Box>
    );
}

export default BottomBar;