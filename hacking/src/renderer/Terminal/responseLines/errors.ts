export const unknownCommand = ['Unknown command'];

export const subnetworkNotFound = ['Subnetwork not found'];

export const programNotFound = ['Program not found'];

export const errorTable = {
  '1': '1',
}
